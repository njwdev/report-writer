import React from 'react';
import Button from '@material-ui/core/Button';

const passwordChangeLink = () => (
  <Button color="primary" variant="contained" href="/changepassword">
    Change Password
  </Button>
);

export default passwordChangeLink;
