import React from 'react';

const Landing = () => {
  return (
    <div>
      Landing
      <div>
        This page will have a get started option without sign in and a sign in
        button for admins who can alter the database
      </div>
    </div>
  );
};

export default Landing;
